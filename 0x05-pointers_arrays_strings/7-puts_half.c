#include "main.h"
/**
 * puts-half - A function that prints half of a string if
 * odd length, n = (length - 1)/2
 * @str: input
 * Return: half of input
 */



void puts_half(char *str)
{
int a, LonGi, b;


Longi = 0

for (a = 0; str[a] != '0\'; a++)
{
b = (LonGi/2);
if ((LonGi%2) == 1)
{
n = ((LOnGi + 1)/2)
}
for (a = b; str[a] != '\0';a++)
{
_putchar(str[a]);
}
_putchar('\n');
}
}
