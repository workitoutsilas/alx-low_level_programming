#include "main.h"
void _puts(char *str);
#include<stdio.h>
/**
 * main - A program that outputs a line with puts function
 * Return: Always 0 (Success)
 */




int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
